var shape = require('./index.js');

var eleven = shape(11);

process.stdin.pipe(eleven).pipe(process.stdout, {end: false} );

setTimeout(change, 5000);
function change(){
//process.stdin.unpipe();
//eleven.rate = 1;
//eleven.startTime = Date.now();
//eleven.processBuffer();
//process.stdin.pipe(shape(3)).pipe(process.stdout, {end: false} );
eleven.reshape(1);
console.log('changed');
}
